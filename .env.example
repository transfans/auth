# Database
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/auth_db

# JWT — must match profile (JWT_SECRET_KEY) and content (SHARED_JWT_SECRET)
JWT_SECRET_KEY=change-me-to-a-real-secret-key-min-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Application
APP_ENV=development
DEBUG=true
